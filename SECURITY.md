# Security Policy

Report vulnerabilities privately through GitHub Security Advisories. Never attach confidential policies, credentials, or personal data. ChangeLens treats documents as untrusted data, validates request size, does not execute document content, and has no outbound model or telemetry integration.

Security fixes are supported for the latest release on `main`.
